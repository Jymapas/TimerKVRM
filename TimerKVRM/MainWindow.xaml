<Window x:Class="TimerKVRM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TimerKVRM"
        mc:Ignorable="d"
        Title="Таймер" Height="350" Width="250" 
        Topmost="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions />
        <Label Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center" Name="Timer" FontSize="120" />
        <UniformGrid Grid.Row="1" Rows="2" Columns="2">
            <Button Content="Вопрос" Name="Question" FontSize="24" Click="Question_Click" IsEnabled="False"/>
            <Button Content="Дуплет" Name="Dupl" FontSize="24" Click="Dupl_Click" IsEnabled="False" />
            <Button Content="Блиц" Name="Blitz" FontSize="24" Click="Blitz_Click" IsEnabled="False" />
            <Button Content="Стоп" Name="StopButton" FontSize="24" Click="Stop_Click" IsEnabled="False" />
        </UniformGrid>
    </Grid>
</Window>
