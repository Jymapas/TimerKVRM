<Window x:Class="TimerKVRM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Таймер" Height="350" Width="250" Topmost="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Label Grid.Row="0"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               FontSize="120"
               Content="{Binding TimerText}"
               Foreground="{Binding TimerBrush}" />

        <UniformGrid Grid.Row="1" Rows="2" Columns="2">
            <Button Content="Вопрос" FontSize="24"
                    Command="{Binding StartQuestionCommand}" />
            <Button Content="Дуплет" FontSize="24"
                    Command="{Binding StartDupletCommand}" />
            <Button Content="Блиц" FontSize="24"
                    Command="{Binding StartBlitzCommand}" />
            <Button Content="Стоп" FontSize="24"
                    Command="{Binding StopCommand}" />
        </UniformGrid>
    </Grid>
</Window>